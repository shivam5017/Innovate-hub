import { memberType } from "./teamType"; // Adjust the import path if necessary
import { projectType } from "./projectType"; // Import the new project type
import { faqType } from "./faqType";

export const schemaTypes = [
    memberType,
    projectType, 
    faqType
];